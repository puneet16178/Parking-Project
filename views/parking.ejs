<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Parking Login</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <section> 
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <h2 class="text-center">Parking Management</h2>
                    <!-- 
                    <table class="table table-hover">
                        <tr>
                            <td>Total Parking:100</td>
                            <td>Used :100-</td>
                        </tr>
                    </table>*/
                    -->
                    <a href="/addnew"><button class="btn btn-info form-control">Add new Entry here</button></a>
                    <table class="table table-hover">
                        <tr>
                            <th>S.No.</th>
                            <th>Vechile Number</th>
                            <th>Vechile Type</th>
                            <th>Vechile In Time</th>
                            <th>Vechile Out Time</th>
                            <th>Amount</th>
                            <th>Status</th>
                            <th>Print</th>
                        </tr>
                        <%record.forEach((result,key)=>{%>
                            <tr>
                                <td><%=key+1%></td>
                                <td><%=result.vno%></td>
                                <td><%=result.vtype%></td>
                                <td><%=result.vin%></td>
                                <td><%=result.vout%></td>
                                <td><%=result.amount%></td>
                                <%if(result.status=='OUT'){%>
                                <td><a href="/parkingupdate/<%=result.id%>"><button disabled><%=result.status%></button></a></td>
                                <td><a href="/parkingprint/<%=result.id%>"><button>Way to Print</button></a></td>
                                <%}else{%>
                                    <td><a href="/parkingupdate/<%=result.id%>"><button><%=result.status%></button></a></td>
                                    <td><a href="/parkingprint/<%=result.id%>"><button disabled>Way to Print</button></a></td>

                                <%}%>

                            </tr>



                        <%})%>
                        
                    </table>
                </div>
            </div>
        </div>
    </section>
    
    
</body>
</html>